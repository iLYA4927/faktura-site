(()=>{const all=window.CATALOG||[];const grid=document.getElementById('catalogGrid');const count=document.getElementById('catalogCount');const search=document.getElementById('catalogSearch');const buttons=[...document.querySelectorAll('[data-filter]')];let filter=new URLSearchParams(location.search).get('type')||'all';function cls(x){if(x.kind==='louver')return'louver';if(x.kind==='profile')return'profile';const s=(x.series||'').toLowerCase();if(s.includes('дерев'))return'wood';if(s.includes('ткан'))return'fabric';if(s.includes('мрам')||s.includes('marble'))return'marble';if(s.includes('кам')||s.includes('лунн'))return'stone';if(s.includes('металл'))return'metal';if(s.includes('кож'))return'leather';if(s.includes('моза'))return'mosaic';return'solid'}function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function price(n){return new Intl.NumberFormat('ru-RU').format(n)+' ₽'}function draw(){const q=(search?.value||'').trim().toLowerCase();const rows=all.filter(x=>(filter==='all'||x.kind===filter)&&(!q||[x.code,x.title,x.series,x.kind_label,x.size].join(' ').toLowerCase().includes(q)));count.textContent='Показано '+rows.length+' из '+all.length+' позиций';grid.innerHTML=rows.map((x,i)=>`<a class="product catalog-product" href="product.html?slug=${encodeURIComponent(x.slug)}"><div class="texture-art ${cls(x)}"></div><div class="product-body"><small>${esc(x.kind_label)} · ${esc(x.series)}</small><h3>${esc(x.title)}</h3><div class="price">${price(x.price)} / ${esc(x.unit)}</div><p>${esc(x.size)}${x.thickness?' · '+esc(x.thickness)+' мм':''}${x.area?' · '+String(x.area).replace('.',',')+' м²':''}</p></div></a>`).join('')}buttons.forEach(b=>b.classList.toggle('active',b.dataset.filter===filter))}buttons.forEach(b=>b.addEventListener('click',()=>{filter=b.dataset.filter;history.replaceState(null,'',filter==='all'?'catalog.html':'catalog.html?type='+filter);draw()}));search?.addEventListener('input',draw);draw()})();